type SoundEffect = 'success' | 'error' | 'like' | 'send' | 'pop';

/**
 * Plays a sound effect. Currently silent;
 * swap in expo-audio or expo-haptics calls here when audio assets are added.
 */
export function playSoundEffect(_effect: SoundEffect): void {
  // Keep silent until audio assets are wired up.
}
