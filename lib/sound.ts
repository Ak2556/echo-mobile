type SoundEffect = 'success' | 'error' | 'like' | 'send' | 'pop';

/**
 * Plays a sound effect. Currently a no-op stub —
 * swap in expo-av or expo-haptics calls here when audio assets are added.
 */
export function playSoundEffect(_effect: SoundEffect): void {
  // no-op until audio assets are wired up
}
